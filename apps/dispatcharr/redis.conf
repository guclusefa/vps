# Redis config for the Dispatcharr AIO internal instance
bind 127.0.0.1
port 6379
protected-mode yes

# Hard memory cap with LRU eviction
maxmemory 64mb
maxmemory-policy allkeys-lru

# Disable persistence — saves RAM and avoids unnecessary disk writes
save ""
appendonly no
